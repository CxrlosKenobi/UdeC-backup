#include <stdio.h>

int main() {
  int i, j, k;
  scanf("num(1) %d", &i);
  scanf("> %d", &j);
  scanf("> %d", &k);


  // If one of them is < 0; Print one of them
  // If two of them are < 0; Print the other one
  // If all of them are < 0; Print all of them
  // If all of them are positive; Do nothing


  
  return 0;
}
